name: Deploy website

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Test
        run: python -m unittest discover -s tests -v

      - name: Build / Package
        run: |
          mkdir -p dist
          cp website/index.html website/styles.css website/app.js dist/
          tar -czf website-package.tar.gz -C dist .
          ls -la dist website-package.tar.gz

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to Amazon S3
        run: aws s3 sync dist/ "s3://${{ secrets.S3_BUCKET_NAME }}" --delete

      - name: Deploy to Amplify Hosting
        env:
          AMPLIFY_APP_ID: ${{ secrets.AMPLIFY_APP_ID }}
        run: |
          cd dist
          zip -r ../website.zip .
          DEPLOY=$(aws amplify create-deployment --app-id "$AMPLIFY_APP_ID" --branch-name main --output json)
          JOB_ID=$(echo "$DEPLOY" | jq -r .jobId)
          UPLOAD_URL=$(echo "$DEPLOY" | jq -r .zipUploadUrl)
          curl -sS --request PUT -T ../website.zip -H "Content-Type: application/zip" "$UPLOAD_URL"
          aws amplify start-deployment --app-id "$AMPLIFY_APP_ID" --branch-name main --job-id "$JOB_ID"
          echo "Published URL: https://main.${AMPLIFY_APP_ID}.amplifyapp.com"

      - name: Invalidate CloudFront cache
        env:
          CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          if [ -z "$CLOUDFRONT_DISTRIBUTION_ID" ]; then
            echo "CLOUDFRONT_DISTRIBUTION_ID is not set; skipping cache invalidation"
            exit 0
          fi
          aws cloudfront create-invalidation \
            --distribution-id "$CLOUDFRONT_DISTRIBUTION_ID" \
            --paths "/*"
